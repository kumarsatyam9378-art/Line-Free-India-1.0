## 2026-04-01 - Missing aria-labels on icon-only buttons
**Learning:** Across the app, there is a recurring pattern of icon-only buttons (like back buttons or notification bells) lacking `aria-label` attributes. This is a common accessibility oversight that screen readers rely on for context.
**Action:** Always verify icon-only buttons have an appropriate `aria-label` added so screen readers provide meaningful context.
